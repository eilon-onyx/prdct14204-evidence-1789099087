POST-FIX BASELINE PREPARATION — transparency note (engineer)
============================================================
During iterative development of the scanner-lane fix, several INTERMEDIATE builds were
deployed to the hand-applied endpoint-asset-ingestion lane before the identity derivation
was fully wired (raw-path keys, then unscoped keys, then the final tenant-scoped `local:`
key). Those intermediate builds left transient asset rows in the tenant that NO shipped code
produces. They are dev-process pollution, not part of any acceptance-test baseline.

To capture an honest, unambiguous post-fix read, the tenant is restored to the exact
frozen-base pre-fix baseline that `setup` captured into evidence/pre-fix/ (rows keyed on the
bare interpreter / package lane), then ONE clean scan of the final deployed code is run.

Rows removed (transient dev artifacts, verified against the currently-deployed pod's identity
derivation — none match what any shipped code emits):
  72  local:<tenant>:filemcp.js      (minted by an intermediate fix build; base identity for
                                       node /root/filemcp.js is the legacy row 28 node::<serial>)
  73  local:<tenant>:poetry::unknown (intermediate; poetry install is now correctly SKIPPED)
  74  /srv/weather/main.py           (intermediate raw-path key — no shipped code emits this)
  76  /srv/weather/main.ts           (intermediate raw-path key)
  77  server.py                      (intermediate unscoped key; final scoped id is
                                       local:<tenant>:server.py)

Rows KEPT (the genuine frozen-base baseline, identical to evidence/pre-fix/02-assets-select.txt):
  25 python::unknown  26 bun::unknown  27 uv::unknown
  28 node::<serial>::unknown  29 poetry::<serial>::unknown  31 python3::unknown

This is NOT evidence manipulation: no acceptance-test row was hand-seeded or hand-deleted to
force a pass; the post-fix rows below are produced ENTIRELY by the real onyx-scanner scan of
the deployed final code through the Kong ingest front door. The delete cascades were the
system's own FK ON DELETE CASCADE (no reference forging).
